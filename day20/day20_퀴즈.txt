VK_ENTER

PROMOTHION

FINAL 수정자

RUN 메서드 : 쓰레드를 실행하기 위한 메서드

출력스트림 : 뱉어내는 빨대

VECTOR : 

SERVER SOCKET : 서버통신위한 객체

JOIN : DB 랑 DB 연결자

내부익명클래스 : 

예외 : 

입력스트림 : 빨아들이는 스트림

default : 접근제한자 설정 안했을 경우

flush() : 버퍼 읽어들이고 엔터 치고 비우기 위해 쓰는것

socket : 통신할때 필요한 객체 / 종이컵

abstract : 추상클래스 

not null 제약조건 : null 이 들어오면 안된다는 오라클 조건

null : 데이터 값이 아무것도 없을 때

import : 다른위치에 있는 클래스 지정 하는것

javaSE : 스탠다드 

runnable : run 을 단독적으로 실행하기위해

스트림 : 데이터의 흐름 

DTO : 클래스인데 , 로직은 없고 데이터만을 담기위한 

JVM : 자바가상머신

추상메서드 : implement 내에는 이것만 있다..

system.out : system 클래스 내의 표준출력 객체? 스트림? 

heap : 인스턴스가 올라가는 영역

unique : 유일한 값을 가지기위한 제약조건

클래스 변수 : static 붙어있는 변수
<-> 안붙어있으면 인스턴스 변수(객체생성 필수)

super 클래스 : 자식 클래스에서 부모클래스를 지칭하는 것

csv 파일 : 콤마로 데이터를 구분하는 파일

object : 최상위 클래스

is a 관계 : ~는 ~이다
(implement, extends)

non-runnable : sleep() 메서들 실행시 이동하는 영역

상수풀 : 암시적으로 올라가는 영역 

rs : 결과집합 

jdbc : 자바언어와 데이터베이스 연동

jdbc 드라이버 : 자바언어가 데이터베이스를 제어하기 위한 라이브러리

jdbc 드라이버의 목적 : db가 바껴도 코드가 바뀌지 않는다..

GOF : 디자인 패턴의 저자들을 가리키는 애칭

SQLLDR : 오라클 설치시 기본적으로 들어있는 마이그레이션 프로그램
마이그레이션 : 데이터 이전

socket : 네트워크의 지식 없이도 네트워크 프로그램을 제어할 수 잇도록
추상화 시켜놓은 객체

디자인 패턴 : 개발자들의 공통적인 개발방법론을 일컬어서 

tablemodel : 디자인과 로직을 분리시키기 위한 jtable이 지원받는 컨트롤러

드라이버매니저 : jdbc 객체중 접속을 시도하는 객체

javaEE : 자바로 개발할 수 있는 개발분야중 기업용 어플리케이션 개발 플랫폼

FileNotFoundException : 지정한 파일을 찾을 수 없다는 예외
(checked exception : 강요받는 예외)

this : 인스턴스가 나 자신을 지칭하는 레퍼런스 변수

system.in : 시스템 객체가 보유하고 있는 표준입력스트림 객체는?
(이미 존재하는 OS로부터 얻어와서 담아놓는다)
 
어댑터 : 이벤트 프로그래밍 시 해당 인터페이스가 보유한 추상메서드가 많을경우
대신 구현해주는 객체 (3개 이상)

entity : 자바에서의 인스턴스 = entity

telnet : 특정 컴퓨터로 원격접속 하기위한 특정 프로그램

아파치 : 오픈소스진영을 이끌고 있는 단체

class.forName : 개발자가 클래스를 프로그래밍적으로 로드하는 메서드
개발자가 원본코드를 static 으로 올릴수 있다

localhost : 자기자신을 가리키는 도메인

포트 : 네트워크 프로그램 간 구분을 위해서 부여된 번호

테이블 모델 리스너 : 

try : 예외 실행에서 시도하는 

iterator : set 유형처럼 순서가 없는 데이터를 순서있게 처리하기 위한 객체

rt.jar : 자바 스탠다드의 클래스들이 압축되어 있는 파일









