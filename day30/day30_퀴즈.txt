localhost : 네트워크에서 나 자신의 컴퓨터를 의미
출력 스트림 : 데이터가 흐르는데 바깥으로 출력하는 흐름
serverSocker : 
javac : 컴파일 할때 치고 출력한다
iterator : 컬렉션 프레임 웍에서 순서있게 만드는 도우미 객체
포트 : 
check 제약조건 : 특정한 조건을 걸어서 체크할 수 있는 조건
non-runnable : sleep() 하는구간
paint() 메서드 : canvas 에서 그릴 때 쓰는 메서드
borderLayout : 동서남북 센터 방위가 있는 배치관리자
drivermanager : 접속할 때 쓰는 객체
flowlayout : 컴포넌트가 물 흐르듯이 배치되는 관리자
primary key : unique + not null
java.util 패키지 : array list 가 있는 패키지
jtable : db를 모델에서 출력할 때 쓰는 객체
/ 스윙에서 테이블 만들때 쓰는 객체

stack 영역 : jvm 에서 메모리영역에서 레퍼런스 변수가 올라가는 영역
heap 영역 : jvm 에서 인스턴스가 올라가는 영역
repaint() : 캔버스에서 다시 그리는 객체
stream : 빨대, 데이터를 빨아들이는 객체
wrapper 클래스 : 각 자료형의 1:1로 대응되는 클래스
join : 두개의 테이블을 물리적으로 연결하는 것
flush() : buffer의 나머지 찌꺼기를 내보내는 메서드
runnable 메서드 : 
그래픽 객체 : paint 메서드에서 객체를 가져올 때 쓰는 객체
인터페이스 : implement
POI 

